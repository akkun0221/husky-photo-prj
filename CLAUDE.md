# CLAUDE.md

このファイルはClaude Codeが会話開始時に自動読込するプロジェクト憲法です。
実装前に必ず参照し、ここに書かれたルールを最優先にしてください。

プロジェクト固有のルールは `documents/project-rules.md` に記載されているため、必ず合わせて読み込むこと。

---

## 基本ルール

- 会話は全て日本語で行うこと（コード・変数名・コメントは除く）
- 実装計画を提示し、承認を得てから実装を開始すること
- 仕様と異なる実装が必要な場合は、先に相談すること
- アーキテクチャや設計を変更する場合は必ず事前に相談すること

---

## セキュリティ

- 秘密鍵・APIキーはサーバーサイドのみで使用する（クライアントコードに含めない）
- 外部ストレージ・外部サービスへの書き込みはサーバーサイド経由のみ許可
- `.env` 系ファイルは `.gitignore` に追加済みであること
- クライアント公開用の環境変数とサーバー専用の環境変数は命名規則で明確に区別する

---

## コーディング規約

- 型を曖昧にしない（TypeScript なら `any` 禁止・`unknown` 使用、Java なら `Object` 型の乱用禁止など）
- 型定義は特定のレイヤー・モジュールに集約する（ファイル末尾に散らばせない）
- ビジネスロジックはエントリーポイント（ルーター・コントローラー）に書かない
- 関数・変数名は言語の慣習に従う（例：JavaScript / Java は camelCase）
- コメントは日本語でOK

---

## 品質確認

- 型チェックとビルドが通ることを実装完了の定義とする（ツールは言語・フレームワークに応じて変わる）
- 実装完了後に手動確認手順を提示する

---

## 実装の進め方

1. 仕様を確認する
2. **実装計画を提示し、承認を待つ**
3. 承認後に実装する
   - **依存の少ない小さい単位から順に実装する**
4. 型チェックとビルドでエラーがないことを確認する
5. 実装報告と手動確認手順を提示する
6. `git push` はユーザーから明示的に指示があった場合のみ行う

---

## 自己改善ルール

### 同じ指示の検知（重要）

同じ会話内で同じ指示・説明を2回受けたら、実装の前に必ず以下を提案すること。

```
【抽象化の提案】
内容：（何が繰り返されたか）
推奨ファイル：（.claude/skills/XX.md 等）
理由：（なぜそのファイルが適切か）
→ 作成しますか？ Yes / No
```

### 抽象化の判断基準

| 繰り返しの種類                 | 推奨ファイル                     |
| ------------------------------ | -------------------------------- |
| 実装手順・作業パターン         | `.claude/skills/`                |
| 保存・コミット時の自動チェック | `.claude/settings.json`（Hooks） |
| レビュー観点（毎回同じ指摘）   | `.claude/agents/`                |
| プロジェクト全体のルール       | `CLAUDE.md` に追記               |

### 会話をまたぐ場合

セッションが変わると記憶がリセットされる。
そのため会話の開始時または終了時に以下を確認すること。

「この会話でSkills / Hooks / Agentsに抽象化すべきものはありましたか？」

---

## やってはいけないこと

- 秘密鍵・APIキーをクライアントコードに含める
- 承認なしで実装を開始する
- 日本語以外で会話する
